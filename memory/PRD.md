# SlotNow Web — Product Requirements & Progress

## Architecture
- React 19 + CRA/craco + Tailwind + React Router 7.
- Backend external at `https://pro-booking-21.emergent.host` (no local backend).
- Booking is SHIFT-based (`start_time == shift.start_time`).
- No websockets → 4s visibility-aware polling.

## Roles (4)
1. **Customer** — browse, book, reschedule, review, refer (with rewards).
2. **Service Provider** — self-onboard (with image upload), services + availability + capacity + assistants, queue, history w/ delete.
3. **Service Assistant** (backend: `receptionist`) — linked-provider queue, walk-ins (with vehicle model), history w/o delete.
4. **Admin** — approve providers (single + bulk), users, bookings, revenue, SMS+payment settings, referral tracking.

## Design (matches mobile app)
- Navy `#1E3A8A` + Orange `#F97316` + cream `#F5F6F8`.
- Custom SVG logo (blue clock + orange checkmark).
- Outfit + Manrope + Noto Sans Devanagari.

## All shipped features

### Iterations 1–7 — Core + roles + design
Customer flow, provider suite, receptionist suite, admin suite, i18n (EN + हिं), PIN re-login, reschedule, automobile vehicle fields, shift-based booking, live polling.

### Iteration 8 — History + walk-in Paid/Free + referral + polish
History page (Provider + Assistant) with Call/WhatsApp/Print + Provider-only Delete. Walk-in Paid/Free segmented control (Provider + Receptionist). Referral tracking via `?ref=`, Admin `/admin/referrals` aggregation. Two-line shift card. Bulk-approve. Image URL preview.

### Iteration 9 — Bugfix + 3 P2 enhancements (100% pass)
- **BUG FIX**: `rec-walkin-model` — added Vehicle Model input to Receptionist walk-in modal (was missing, blocking automobile walk-in bookings).
- **Native image upload**: ProviderOnboarding now has an `Upload image` button that reads any local image file, compresses it (max 800px, JPEG q=0.75) to a base64 data URL via `compressImageToDataURL` helper, and stores it in the profile. Preview + Clear button included. URL paste still supported as fallback.
- **Force onboarding for empty profile**: `Login.homeForRole` now checks both `/providers/me/profile` 404 AND empty `business_name` → routes to `/provider/onboarding`. Verified: brand-new provider phone 8782913356 lands on onboarding, not dashboard.
- **Referral rewards progress**: Customer Profile Refer & Share card now shows a 3-tier progress bar (1 = ₹50 credit, 3 = ₹200 booking, 10 = priority support). Tries `GET /api/referrals/mine` for live count, falls back to 0 gracefully when backend doesn't expose the endpoint (currently 404s).

### Iteration 10 — WhatsApp + Calling + DnD + Admin controls (100% pass, Feb 2026)
- **WhatsApp deep link + templates**: New `WhatsAppModal` (`/app/frontend/src/components/WhatsAppModal.jsx`) with 4 pre-filled templates (Confirmation, Reminder, Follow-up, Referral Invite) plus Manual. `waLink(phone, text)` builds `https://wa.me/91<phone>?text=<encoded>`. Uses `window.location.origin` for the invite URL (no hardcoded preview host).
- **Call deep link**: `tel:+91<phone>` button next to WhatsApp on every queue row and history row.
- **Drag-and-drop queue reorder (Receptionist)**: `@dnd-kit/core` + `@dnd-kit/sortable` on `ReceptionistDashboard`. Drop → POST `/api/queue/reorder` with `{ date: todayISO(), ordered_ids }`. Uses local (not UTC) date via `todayISO()` in `utils-app.js` to avoid the IST off-by-one after midnight IST.
- **Reusable QueueRow**: `/app/frontend/src/components/QueueRow.jsx` — used by both Provider Queue and Receptionist Dashboard. Provider queue intentionally does NOT render drag handles (drag_handles=0) — reorder is receptionist-only.
- **Admin Suspend/Unsuspend + Approve/Reject**: `AdminDashboard` gains suspend/unsuspend buttons on approved providers (currently maps to reject endpoint pending a dedicated backend endpoint — captured in Open Backend Recommendations).
- Test IDs added: `wa-modal`, `wa-option-{k}`, `wa-manual-input`, `wa-preview`, `wa-send-btn`, `queue-drag-<id>`, `queue-call-<id>`, `queue-wa-<id>`, `history-whatsapp-<id>`, `history-call-<id>`, `admin-suspend-<id>`, `admin-unsuspend-<id>`, `admin-approve-<id>`, `admin-reject-<id>`, `admin-bulk-approve-btn`.

## Test coverage summary
- Iteration 1: 12/12 customer.
- Iteration 4: 13/14 — roles + design.
- Iteration 7: 4/4 (100%) — Automobile end-to-end.
- Iteration 8: 11/12 + UI-verified bulk approve.
- Iteration 9: 6/6 (100%) — all P2 items + bug fix verified.
- Iteration 10: 100% — WhatsApp + Call + DnD + Admin controls; only 2 non-blocking design nits.
- Iteration 11: 100% — post-fix verification of `todayISO()` local-date + WhatsApp `window.location.origin` invite.
- Iteration 12: 100% — code-quality refactor sweep (hook deps, empty catches, useMemo, stable keys). Zero regressions.
- Iteration 13: 6 user-reported bugs fixed — schedule date (nextNDays local), ↑/↓ arrow buttons on receptionist queue, patient-history modal on row click, on-time alert on BookingDetail, walk-in conditional fields, Get Directions link. Surfaced 2 backend gaps.
- Iteration 13: 6 user-reported bugs fixed — schedule date (nextNDays local), ↑/↓ arrow buttons on receptionist queue, patient-history modal on row click, on-time alert on BookingDetail, walk-in conditional fields, Get Directions link. Surfaced 2 backend gaps.
- Iteration 14: 100% — workarounds for the 2 backend gaps (position derived from `qp.wait`, map link co-stored in address).
- **Iteration 15: 5/5 PASS** — mobile-app parity round: (1) weekday convention bridge JS↔Python fixes "schedule shifts to next day", (2) LocationPickerModal for provider onboarding (Use current location / Open Google Maps / paste), (3) Automobile branch on walk-in modal now uses `providerInfo.category` string (not category_id), (4) non-auto uniformity preserved, (5) HistoryPage rows now open PatientHistoryModal on name click.
- **Iteration 16: 5/5 PASS** — LocationPickerModal geolocation UX overhaul. Root cause of "Location permission denied" on Android was Emergent's chat preview loading the app in a cross-origin iframe without `allow="geolocation"`. Fixes: iframe detection with proactive amber notice + "Open in a new tab" button, Permissions API preflight, retry-with-lower-accuracy on TIMEOUT, 5 distinct `data-error-kind` classifications (iframe/denied/unavailable/timeout/other) each with actionable copy.
- **Iteration 17: 100% PASS** — Login page now offers Email + Password sign-in (DEV OTP bypass) via `POST /api/auth/login-email`. Root cause of user-reported "mock OTP not working" was the backend team's live upgrade: demo `123456` fallback removed, SMS gateway respects the admin toggle. Since MSG91 is currently disabled, OTP is unavailable — so we added the alternative login path the backend already exposed. Any user with email credentials (admin especially) can now sign in and enable MSG91.
- **Iteration 18/19: FIXED (100%)** — user reported "backend still returns demo OTP" and "DLT Template ID missing". Root cause: (a) a STALE amber banner in AdminSettings hardcoded from iter15 said "backend still returns demo OTP (123456)" — misleading, because Emergent backend team had already shipped MSG91 + removed demo OTP; (b) DLT field existed in code but the user was on production which hadn't been redeployed. Fixes: removed stale banner, added dynamic live/off status card, added "Send test SMS" section wired to `/admin/settings/sms/test-send`, removed the `123456` phrasing from the Mock provider dropdown label. bug_testing_agent verdict: `fixed`.
- **Iteration 20: FIXED (100%)** — user reported "DLT Entity ID field missing from SMS settings". Root cause: backend `SmsSettings` schema has THREE DLT fields (`dlt_template_id`, `dlt_entity_id`, `dlt_variable_name`) but frontend only rendered `dlt_template_id`. Fix: added `admin-setting-dlt-entity-id` (digit-only, 19-digit Principal Entity ID / PEID) and `admin-setting-dlt-variable-name` (alphanumeric, default "num") inputs. All three fields now included in the save PUT body — verified via network intercept by bug_testing_agent.
- **Iteration 21: FIXED (100%)** — user reported "DLT fields blocking alphabets". Root cause: iter20 added `e.target.value.replace(/\D/g, '')` sanitizer that stripped every letter. Fix: removed all character-class restrictions on all three DLT inputs, removed `inputMode="numeric"` — fields now accept raw string as typed/pasted.
- **Iteration 22: 8/8 PASS** — user asked for backend MSG91 API fix. Backend cannot be modified (external, no source access). Instead added diagnostic X-ray UI on `/admin/settings/sms`: new **Dry-run** button (POSTs `/admin/settings/sms/dry-run`, shows exact payload backend would send to MSG91 without dispatch) + updated **Send test** button to render raw response — both now surface backend responses inside `admin-settings-diagnostic-panel` (rose-tinted for errors, HTTP status labeled). Gives the user concrete evidence to hand to Emergent Support when SMS silently fails.
- **Iteration 23/24: FIXED (100%)** — user reported dry-run showed 10-digit phone (no `91` prefix), causing MSG91 to reject SMS. Frontend curl-verified that the backend passes through whatever phone string the client sends. Fix: new `/app/frontend/src/lib/phone.js` exports `toIndianE164()` (handles 10-digit, 12-digit-with-91, 11-digit-with-leading-0, and formatted variants). Wired into `AuthContext.sendOtp/verifyOtp/pinLogin` and `AdminSettings.sendTestSms/dryRunSms`. Iter24 also fixed AdminSettings input to stop pre-truncating to 10 digits (validate normalized output instead). All 13 assertions pass — every SMS + auth outgoing request now carries `919876543210` regardless of input format.
- **Iteration 25: FIXED (20/21 = 95%)** — user reported Dry-run showing "HTTP 404 detail Not Found". Root cause: backend rolled back overnight — `/api/admin/settings/sms/dry-run`, `/test-send`, and `/api/auth/login-email` endpoints removed (verified via live curl); `demo_otp: 1234` fallback also came back. Fix: converted raw 404 errors on all three buttons into clean, actionable messages explaining the endpoint isn't deployed and what to do (use OTP flow). Non-404 errors still show raw backend responses for real debugging.
- **Iteration 26: FRONTEND-ONLY MITIGATION (7/7 mocked pass) — backend restoration still pending backend team.** User requested restoring three backend endpoints; I cannot modify the deployed backend. Instead added a defensive frontend adaptation: new `useBackendCapabilities()` hook fetches `/openapi.json` once at mount, and gates the Dry-run / Send Test / Email-login buttons on whether their endpoints exist.
- **Iteration 27/28: FIXED (20/20 backend, 100%)** — user shared the backend source repo (`book-preview-11.zip`). Extracted the real `server.py` (1473 lines) into `/app/backend/`. Added: `normalize_indian_phone()` helper, `_build_msg91_payload()` + `_dispatch_msg91()` (real httpx POST to `https://control.msg91.com/api/v5/flow/`), rewrote `/auth/send-otp` to fire live MSG91 when settings.enabled+provider=msg91+api_key present (no demo_otp returned in live mode), added `POST /admin/settings/sms/dry-run` (redacted payload preview) and `POST /admin/settings/sms/test-send` (real MSG91 fire), extended `SmsSettings` model with `dlt_entity_id` + `dlt_variable_name`, and gated `verify_otp` mock fallback so `MOCK_OTP=123456` is REJECTED in live mode (only stored OTP counts). Verified via real MSG91 API calls returning `type:success`. User must copy `/app/backend/server.py` into their repo and redeploy pro-booking-21.emergent.host.
- **Iteration 29: FIXED (P0 unblock)** — user was locked out because MSG91 SMS wasn't arriving and demo OTP had been removed. Added a **Bootstrap Admin PIN Login** to `/auth/pin-login`: when `BOOTSTRAP_ADMIN_PHONE` + `BOOTSTRAP_ADMIN_PIN` env vars are set, hitting pin-login with matching phone + PIN + role=admin returns a valid JWT immediately (auto-creates the admin user on first hit, or lazily persists the pin_hash if the user existed without a PIN). Also made the underlying pin-login lookup phone-format agnostic (matches both raw & 12-digit). Env: `BOOTSTRAP_ADMIN_PHONE=9412575970`, `BOOTSTRAP_ADMIN_PIN=1234`. Verified locally via 4 curl cases (correct PIN → token; wrong PIN → 401; 12-digit phone → same user; wrong role → 401). **User action required: copy updated `/app/backend/server.py` + set the two env vars on `pro-booking-21.emergent.host` and redeploy.**
- **Iteration 30: DEPLOYED (Prod live @ slotnow.co.in)** — user deployed backend to production. Fixed 2 deploy blockers: (1) added unprefixed `/health` and `/` root endpoints in `server.py` so K8s liveness probes stop getting 404s; (2) fixed seed KeyError `'Doctor'` by changing sample provider `cat` to `"Healthcare"` (matches the renamed category). Post-deploy: bootstrap PIN login worked on prod, but production Mongo was empty of SMS settings — saved MSG91 config directly via authenticated admin API (`PUT /api/admin/settings/sms`) with user-provided credentials (api_key `548721AhSyuiRtc6a547edcP1`, sender `SLOTNW`, template `6a5f8da0d56797446f0612d2`, PEID `1277178456195361134`, var `num`). Verified real MSG91 send-otp on prod returns `provider_used: msg91`, user confirmed OTP arrived. Deployment agent PASS.
- **Iteration 31: LANDING + SEO + LAYOUT (Feb 2026)** — user requested professional revamp. (1) Built new **public landing page** at `/` with hero (tagline "Book appointments in seconds"), Why-SlotNow features grid, live categories grid (from `/api/categories`), "How it works" 3-step section, live featured providers grid (from `/api/providers`), provider signup CTA, and footer with phone/email/WhatsApp links. Top-right **Login dropdown** with 4 role options (Customer/Service Provider/Service Assistant/Admin) linking to `/login?role=<role>`. (2) Moved authenticated customer home from `/` to `/home`; updated `Login.jsx` default redirect, `AppShell` BottomNav home link, and `MyBookings` empty-state navigate. (3) `Login.jsx` now reads `?role=` query param and preselects role. (4) **Widened AppShell** from `max-w-3xl` to `max-w-4xl md → max-w-6xl lg` so desktop dashboards no longer feel cramped. (5) **SEO**: rewrote `public/index.html` with proper `<title>`, `description`, `keywords`, canonical, Open Graph (fb/whatsapp preview), Twitter card, JSON-LD Organization+WebSite schema, favicon set to `/logo.png`. Created `public/robots.txt` (allow public, disallow protected routes, points to sitemap) and `public/sitemap.xml`. (6) Placed user-provided logo at `/logo.png` for the app header, hero card, footer, favicon and og:image. Verified via screenshots on desktop (1440px) and mobile (390px) — all sections render, categories + providers load from DB, admin login flow still redirects correctly to `/admin`, all API responses 200 OK.
- **Iteration 32: PUBLIC PROVIDER PAGES + TESTIMONIALS (Feb 2026)** — user asked for SEO-friendly no-login provider pages and testimonials. (1) Created **`PublicProviderPage`** at `/p/:id` — fetches `GET /api/providers/{id}` (already public, no auth). Renders hero (image, category badge, rating, city, price, bio, Book/Call CTAs), Services list, Reviews section, Location sidebar with Google Maps link, and "Ready to book?" sidebar card. **Dynamic SEO** via `useSeoTags` hook — sets per-provider `<title>`, meta description, OG tags, Twitter card, canonical URL. Also injects JSON-LD `LocalBusiness` schema (name, image, address, aggregateRating, priceRange). "Book appointment" click routes via `/login?role=customer` with React Router state so the user lands on `/book/{id}` after logging in. (2) Landing page **provider cards now link to `/p/{id}`** (was `/login?role=customer`) — visitors can see provider details before signing up. (3) Added **Testimonials Section** to landing (`#testimonials`) — 6 quote cards in 3-column grid with 5-star ratings, colored avatar initials, customer + provider mix. (4) Registered new route in `App.js`. Verified via screenshots: title="Sharma Clinic — Healthcare in Mumbai | SlotNow", meta desc set correctly, canonical set to slotnow.co.in domain, JSON-LD schema injected, Book CTA redirects to login with correct next path.
- **Iteration 33: DYNAMIC SITEMAP + REVIEWS CTA + CATEGORY PAGES (Feb 2026)** — 3 SEO enhancements: (1) **Backend `GET /api/sitemap.xml`** — dynamic XML sitemap includes homepage, login variants, all active categories as `/c/:slug`, and all approved providers as `/p/:id`. Updated `public/robots.txt` Sitemap directive to `https://slotnow.co.in/api/sitemap.xml`. Deleted the old static `public/sitemap.xml`. Verified live: returns 18 URLs currently. (2) **Backend `GET /api/providers/{id}/reviewable-booking`** (auth required) — returns `{eligible, booking_id}` if the current customer has a completed unreviewed booking with this provider. Added `MessageSquare` "Leave a review" button on `PublicProviderPage` reviews header. Full modal with 5-star selector + comment textarea (500 char limit) + POST `/api/reviews`. Not-logged-in click → redirects to `/login?role=customer` with `from` state; non-customer role → error toast; ineligible → informative toast; success → toast + auto-refresh. (3) **Backend `GET /api/categories/by-slug/{slug}?city=`** — returns category + approved providers + top cities aggregation (for city chip filters). Created **`CategoryPublicPage`** at `/c/:slug` with hero (category color badge + name + provider count), search input, clickable city chips (with counts), and provider grid. Full SEO: `<title>` = "Healthcare — Book appointments online | SlotNow" (or "Healthcare in Mumbai" when city filtered), canonical honors city query, meta description, JSON-LD `CollectionPage` with `ItemList` of top 20 providers. City filter uses `?city=` URL param so each city gets a unique canonical URL for long-tail SEO. Added `slugify` helper on Landing so category cards link to `/c/:slug` (matches backend). Verified all 7 seed category slugs return correct providers, /c/healthcare page renders with search + city chips + provider grid, Leave-a-review button on `/p/:id` correctly redirects to login when logged out.
- **Iteration 36: RAZORPAY SUBS + HOSPITAL STAFF + ASSIGNMENTS + GEO-INDEX (Feb 2026)** — 4-feature batch. (1) **Razorpay provider subscriptions** (customer bookings stay free): backend 5 endpoints (`GET /subscriptions/plans`, `GET /subscriptions/me`, `POST /subscriptions/create-order`, `POST /subscriptions/verify`, unprefixed `POST /api/subscriptions/webhook`), 3 seeded plans (Basic ₹499/30d, Pro ₹1299/90d, Business ₹3999/365d), server-side signature + webhook signature verification (`razorpay` SDK), env vars `RAZORPAY_KEY_ID/SECRET/WEBHOOK_SECRET` (placeholders in .env). Frontend `SubscriptionUpgrade` component (Upgrade to Pro card + plan selection modal + Razorpay Checkout via CDN script) added to Provider Dashboard. (2) **Hospital-managed staff**: new `HospitalStaff` model (`kind: doctor | service`, name, specialization/service_tags, photo, address, lat/lng) + 5 endpoints (`GET/POST/PATCH/DELETE /providers/me/staff` gated to `provider_type=hospital`, plus public `GET /providers/{id}/staff`). New page `HospitalStaff.jsx` at `/provider/staff` with add-doctor / add-service modals (photo upload uses `compressImageToDataURL` from lib/image), specialization dropdown, service multi-select chips, geolocation fetch. Shortcut card on Provider Dashboard only when `provider_type === "hospital"`. (3) **Assistant multi-staff assignment**: `PUT /providers/me/assistants/{id}/staff` with `staff_ids[]` validation. Extended `ProviderAssistants` page with per-assistant ListChecks button opening `AssignModal` — grouped doctors/service centers with checkboxes. Empty selection = full access. (4) **Advanced nearby with MongoDB 2dsphere**: added `location: {type:"Point",coordinates:[lng,lat]}` GeoJSON write on provider profile save, `db.providers.create_index([("location","2dsphere")])` on startup, and rewrote `/api/search/providers` nearby branch to use `$geoNear` aggregation (spherical, distanceField, maxDistance). Automatic Python-side Haversine fallback if index missing/query fails. Distance now returned as `distance_km` (2 decimal). Verified: sitemap 28 URLs, `/api/subscriptions/plans` returns 3 plans (basic/pro/yearly), create-order returns 503 when Razorpay keys missing (expected), reference/healthcare returns 3 types + 16 specs + 12 services, `/provider/staff` route registered (200), `$geoNear` index created on startup.
- **Iteration 37: LOGIN REDESIGN + HOSPITAL STAFF ON PUBLIC PAGE + SUB-BOOKING + ONBOARDING POLISH (Feb 2026)** — 4 items. (1) **Full-page Login redesign**: replaced the floating mobile-card layout with a 2-column split — left panel (lg+): forest-gradient brand hero with logo, "Book appointments in seconds", 3 features + copyright; right panel: form section with "Welcome back / Sign in to your SlotNow account" header, role grid, existing OTP/PIN/email flows intact. Mobile stays as clean single column with the SlotNowMark. Post-login `navigateAfter` already routes to role-appropriate dashboards. Verified end-to-end: admin PIN login → `/admin` in one hop. (2) **Hospital staff on `/p/:id`**: `PublicProviderPage` fetches `GET /providers/{id}/staff` when `provider.provider_type === "hospital"`. Renders "Our doctors" grid (photo + specialization) and "Diagnostic services" grid (name + service tags chips + address). Clicking a doctor/service navigates to `/login?role=customer` with `from: /book/:providerId?staff=<id>` state. (3) **Hospital sub-doctor booking**: extended `Booking` + `BookingCreate` models with `staff_id`, `staff_name`, `staff_kind`. When customer books with `?staff=xxx` query param, `BookSlot` passes it in payload; backend enriches the booking with the referenced HospitalStaff details (validated against hospital). (4) **Provider onboarding polish**: added progress indicator bar + 3 step chips (Basics / Location / Photo) at top of form, and hospital-type providers now redirect to `/provider/staff` after saving (natural next step to add doctors). Verified all screenshots and API flows.
- **Iteration 38: PER-DOCTOR QUEUES + FEATURED PLACEMENT + BOOKSLOT PICKER (Feb 2026)** — 3 items. (1) **Per-doctor slot limits**: `HospitalStaff` model + Upsert gained `daily_slot_limit: Optional[int]`. `HospitalStaff.jsx` add-modal has a "Daily slot limit" number input. Booking creation now validates: if `staff_id` present, checks the referenced HospitalStaff exists+active (400 if not), enforces the per-staff daily limit against `bookings` count (status confirmed/completed), and assigns an **independent token number** per (provider, staff, date) so each doctor has its own queue position sequence (rather than a shared hospital counter). (2) **Featured placement for active subscribers**: helpers `_pro_provider_ids()` (queries `subscriptions.active=true, expires_at>now`) and `_sort_with_featured()` inject `is_featured: bool` and sort so subscribed providers surface first (rating breaks ties). Applied in `/search/providers` (default + $geoNear branches — nearby uses 1km distance buckets so proximity still dominates but featured wins ties), `/categories/by-slug` provider list, and `/city/{slug}` grouped providers. Frontend `CustomerSearch` cards render a small orange "★ Featured" pill when `p.is_featured`. (3) **BookSlot doctor/service picker**: when the loaded provider is a hospital, `BookSlot` fetches `/providers/{id}/staff` and shows a chip grid picker above the service section. Selection updates `?staff=` URL param + drives the booking `staff_id`. Guard: submit blocked with a toast until a doctor/service is picked. Preselect honors `?staff=` from the URL. Verified: search endpoint returns `is_featured` field, `/city/mumbai` still groups correctly, `_haversine_km` fallback intact for missing 2dsphere case.

- **Iteration 39: PER-DOCTOR / PER-SERVICE WEEKLY SCHEDULES (Feb 2026)** — Priority 2 delivered end-to-end. Every hospital sub-doctor and sub-service now maintains its OWN independent weekly time-of-day schedule, and this is reflected during the customer booking flow. (1) **Data model**: `AvailabilityRule` + `AvailabilityCreate` gained optional `staff_id`. `staff_id=None` = hospital/provider-wide default (backward compat); `staff_id=<id>` = that doctor's/service's private shifts. (2) **Endpoints added**: `GET/POST/DELETE /api/providers/me/staff/{staff_id}/availability` (provider-authenticated CRUD, hospital-scoped), plus public `GET /api/providers/{id}/staff/{staff_id}/availability`. Provider-wide endpoints (`/providers/me/availability` GET/POST/DELETE) now only touch rules where `staff_id` is null/missing so per-staff rules never leak or get deleted by accident. (3) **`GET /providers/{id}/slots?date=&staff_id=`**: when `staff_id` is passed, returns shifts for that staff on that weekday; if the staff has no shift for that weekday, falls back to the hospital default (with `schedule_scope: "hospital_default"`). Otherwise returns provider-wide default. Booked counts are scoped per staff so each doctor's queue capacity is independent. (4) **Booking validation**: `POST /bookings` resolves the shift against staff-specific → hospital-default → legacy (missing key) in that order, and enforces `max_bookings` per (provider, staff, date, start_time). (5) **Frontend**: new page `/provider/staff/:staffId/schedule` (`HospitalStaffSchedule.jsx`) — per-weekday shift editor with start/end time, slot duration, max bookings; grouped display + remove. `HospitalStaff.jsx` shows a **Schedule** link (`data-testid staff-schedule-{id}`) on each doctor/service row. `BookSlot` now includes `staff_id` in the slots API call and refetches when the selected doctor/service changes, so customers always see the exact time slots for the specific doctor/service they chose. (6) **Test coverage**: `/app/backend/tests/test_hospital_staff_schedule.py` (14/14 pytest cases pass) covers per-staff CRUD, provider-wide-leak prevention, staff-specific slots, hospital-default fallback, provider-scope fallback without staff_id, cross-doctor independence, and per-staff booking validation. Regression on `/admin/stats`, `/admin/subscription-analytics`, `/admin/referrals` all 200. Frontend E2E verified via testing_agent_v3_fork — 100% pass.

- **Iteration 40: ADMIN LOCKDOWN + AVAILABILITY OVERRIDES + QUEUE-SMART REMINDERS + PROVIDER ANALYTICS (Feb 2026)** — 4 features shipped. (1) **Admin lockdown**: `_admin_allowed_phone_set()` reads `BOOTSTRAP_ADMIN_PHONE` and `ADMIN_ALLOWED_PHONES` env (comma-separated, normalised via `normalize_indian_phone`). `_assert_admin_phone_allowed(phone)` is called at the TOP of both `/auth/pin-login` and `/auth/verify-otp` when `role=="admin"` — before the OTP/PIN is even checked. Any non-allowed phone gets HTTP 403 "Admin access is restricted to authorized personnel only." Config: `ADMIN_ALLOWED_PHONES=9412575970` added to `backend/.env`. (2) **Availability Overrides**: new `AvailabilityOverride` model + `availability_overrides` collection with `(provider_id, staff_id?, date, kind, start_time?, end_time?, max_bookings?, note?)`. `kind="closed"` marks the whole day off, `kind="shift"` provides a one-off replacement shift (requires start_time+end_time). Endpoints: `GET /providers/me/overrides?staff_id=&from=&to=`, `POST /providers/me/overrides`, `DELETE /providers/me/overrides/{id}`. `GET /providers/{id}/slots` checks overrides FIRST — closed returns `has_schedule:false schedule_scope:"override_closed"`, shift returns `schedule_scope:"override_shift"` with only override shifts. `POST /bookings` respects the same order: closed → 400 "unavailable", shift → validated against override times. Frontend: `HospitalStaffSchedule.jsx` gained a "Per-date override" section with On leave / Extra shift toggle, date + time inputs + note, and an "Upcoming overrides" list with delete buttons. (3) **Queue-based smart reminders**: helper `_notify_queue_positions(provider_id, date, staff_id?)` computes each active booking's position by sorting `token_number` and fires the 20-ahead + 3-ahead SMS (via `_send_reminder_sms_msg91` using a SECOND DLT template configured on the sms settings row as `reminder_template_id` / `reminder_var_ahead` / `reminder_var_name`). Idempotency via `reminder20_sent` / `reminder3_sent` booleans on each booking. Also inserts a record into `queue_reminders` for auditing (with `sent`, `note` — when the template isn't configured, `sent=false` and note explains). Triggered from `POST /bookings` (for the new booking), `PUT /bookings/{id}` when status transitions to completed/cancelled/rejected/no_show, and `POST /queue/next` (advances current_token). Sends an in-app `push_notification` mirror. (4) **Provider Analytics**: `GET /providers/me/analytics?days=30&staff_id=` returns totals (total/completed/cancelled/no_show/walkin/rejected/confirmed/pending), 7×24 weekday-hour heatmap based on `start_time`, per_staff breakdown (each with its own heatmap + utilisation), and provider-level `capacity` (calculated by summing per-weekday shift `max_bookings` — or minutes/slot_duration when unlimited — multiplied by weekday occurrences in the window) + `utilisation_pct`. New page `/provider/analytics` with KPI cards, utilisation bar, weekly heatmap grid, range selector (7/30/90 days), staff selector (hospital only), and per-doctor drill-in. Dashboard link `provider-open-analytics`. (5) **Test coverage**: `/app/backend/tests/test_iter40_overrides_reminders_analytics.py` — 18/18 backend pytest + frontend E2E all pass via testing_agent_v3_fork.

- **Iteration 41: SMS REMINDER TEMPLATE UI + BULK SCHEDULE GRID + CUSTOMER LIVE QUEUE POSITION (Feb 2026)** — 3 features shipped. (1) **Admin SMS settings** gained 3 optional fields exposed in `AdminSettings.jsx` under a new "Queue reminder template (optional)" block: `admin-setting-reminder-template-id`, `admin-setting-reminder-var-ahead`, `admin-setting-reminder-var-name`. DLT template still pending user's approval — form saves fine, dispatch path already wired from iter40. (2) **Bulk Availability Editor** `/provider/schedule-grid` (`HospitalScheduleGrid.jsx`): matrix view of ALL sub-doctors/services (rows) × 7 weekdays (columns). Per-cell shifts are rendered as pills with start-end · max-bookings; hover reveals a trash icon (`grid-remove-{ruleId}`). Every cell has an `+ Add` button (`grid-add-{staffId}-{jsDay}`) opening a modal that POSTs `/providers/me/staff/{id}/availability`; cells with existing shifts also have a `Copy` button (`grid-copy-{staffId}-{jsDay}`) opening a target-day multi-select — one click clones every shift of that day to every selected target day. New Provider Dashboard card `provider-schedule-grid` shown side-by-side with `provider-manage-staff` (hospital only). (3) **Customer Live Queue View**: `GET /queue/my-position` rewritten to compute `ahead` correctly for per-staff hospital bookings — counts active pending/confirmed non-walkin bookings in the same `(provider_id, staff_id, date)` with a smaller `token_number`. `BookingDetail.jsx` now shows a prominent `booking-queue-position` pill (green when position=0 → "🟢 You're up next!", otherwise "You are #N in queue · X ahead"), plus an "Auto-refreshing every 4s" note under the now-serving line. useLivePolling continues driving the 4s auto-refresh. (4) **Test coverage**: `/app/backend/tests/test_iter41.py` — 8/8 backend pytest pass (with 1 correctly skipped), iter40 regression pytest still 18/18 pass, frontend E2E via testing_agent_v3_fork verified all 3 flows end-to-end.

- **Iteration 42: LOGO UNIFORMITY + CATEGORY-AWARE FIELDS + HOSPITAL SPLIT + "OTHER" SPECIALIZATION + HOME FILTERS (Feb 2026)** — 6 surface fixes shipped in one pass. (1) **Logo consistency**: dropped the SVG `SlotNowMark` from `Login.jsx` mobile brand; every page (Login, LandingPage, PublicProviderPage, CityPublicPage, CategoryPublicPage, CustomerSearch) now uses `/logo.png` at a uniform `h-12 w-12` (was h-9/h-10). Mobile login logo bumped to `h-20 w-20`. (2) **Vehicle fields gated by category**: new `/app/frontend/src/lib/providerType.js` with `isAutomobileProvider(provider)` matching on category NAME (case-insensitive, since seeded category ids differ across environments). `BookSlot.jsx` now sets `category_name` from the API's `category.name` and drives `isAutomobile`. `ProviderQueue.jsx` walk-in modal and `ProviderServices.jsx` service-type input are only visible when the provider is Automobile — `hidden` Tailwind class is used for the service-type input so the DOM slot exists but stays invisible. (3) **Hospital booking picker split**: `BookSlot.jsx` staff picker now renders TWO labeled groups — 'Doctors' and 'Other Services' (data-testids `staff-group-doctor` / `staff-group-service`) — instead of one mixed grid. `HospitalStaff.jsx` section heading renamed from 'Service centers' → 'Other Services'. (4) **"Other" specialization**: `HospitalStaff.jsx` doctor modal and `ProviderOnboarding.jsx` clinic self-signup gained an `__other__` option in the Specialization dropdown that reveals a free-text input (`staff-spec-other` / `onboarding-specialization-other`) when chosen. Backend already accepts any string. UI-only `specialization_choice` helper field is stripped before the POST. (5) **Customer Home filters**: `/home` now has `home-nearby-btn` (Locate icon, uses `navigator.geolocation` and re-fetches `/search/providers?lat=&lng=&limit=60`) and `home-doctor-type` select seeded from `/reference/healthcare` specializations (re-fetches with `?specialization=…`), plus `home-filters-clear`. (6) **Test coverage**: `/app/backend/tests/test_iter42.py` (6/6 pass) covers `/providers/{id}` `category.name`, freeform Other-specialization on `POST /providers/me/staff`, `/search/providers` distance sort. Regression: iter40 18/18, iter41 8/8, iter39 13/14 (1 test-data conflict on retry — not code). Frontend E2E via testing_agent_v3_fork verified all 6 fixes end-to-end.

- **Iteration 43: SERVICE/DOCTOR PHOTO UPLOAD + CUSTOMER WAIT-TIME HISTORY (Feb 2026)** — 2 features shipped. (1) **Photo upload on services & doctors**: `Service` model + `ServiceCreate` gained optional `photo` (data URL). `ProviderServices.jsx` add-form has a `service-photo-input` file picker (compressed via `compressImageToDataURL` maxDim 800, quality 0.72) with a live preview + remove button, and every service card renders a w-14 h-14 thumbnail when set. Customer-facing `BookSlot.jsx` service picker card shows a 48px thumbnail when the service has a photo. The hospital sub-doctor / other-service picker pill was redesigned as an avatar row: `<img>` when `staff.photo` exists, else a first-letter fallback badge. (Doctor `photo` was already in the HospitalStaff model — no schema change needed there, just the display.) (2) **Customer wait-time history**: `Booking.completed_at` (optional UTC datetime) added and stamped in both `POST /queue/next` (auto-complete on advance) and `PUT /bookings/{id}` when transitioning to status=completed. New endpoint `GET /customers/me/wait-history?limit=` returns `count`, `counted_for_avg`, `avg_wait_min`, `history[]` (with `waited_min: max(0, (completed_at - scheduled_slot).minutes)`), `by_weekday`, `by_hour`, and a `hint` sentence (`"Try booking around HH:00 — your avg wait there is only ~X min vs ~Y min at ZZ:00."`) that is populated only when `counted_for_avg ≥ 3` AND best hour ≠ worst hour. `_enrich_bookings` now injects `waited_min` on every completed booking (null for pre-migration rows). Home page shows a `home-wait-history-widget` gradient card (`wait-avg-min` big number + optional `wait-hint`) that navigates to `/bookings?tab=completed`. `MyBookings.jsx` 'Past' tab renders a color-coded `booking-waited-{id}` pill (emerald ≤15, amber ≤30, rose >30). (3) **Test coverage**: iter43 pytest 9/9 pass (2 skipped by design), full regression 46/47 (1 pre-existing test-data conflict), frontend E2E via testing_agent_v3_fork verified all 4 UI surfaces.

- **Iteration 44: COMPREHENSIVE SEARCH & FILTER OVERHAUL (Feb 2026)** — Home page search + filter fully upgraded. (1) **Global name search**: `/search/providers?q=` now unions provider fields (business_name, specialization, bio, service_tags) with hospital_staff (name, specialization) and services (name) matches via provider_id set — so "Cardio" surfaces the hospital whose sub-doctor is a cardiologist, "Full Detailing" surfaces the auto shop whose service catalog has it. (2) **Smart city / Near-me**: new `GET /reference/cities?q=&limit=` returns distinct sorted city list from approved providers; Home page has a `home-city-input` with a live typeahead dropdown (`home-city-suggestions` + `home-city-opt-{name}`), plus the existing `home-nearby-btn` for GPS-driven distance sort. (3) **Category-aware secondary filter**: new `GET /reference/filters?category_id=` returns `{ label, param, options }` — Healthcare → `Doctor type / specialization` (DOCTOR_SPECIALIZATIONS, param `specialization`), Automobile → `Vehicle service` (7 options, param `service`), Salon/Tutor/Consultant/Coach/Home Service each have their own dynamic option lists keyed by category NAME (lowercase). Frontend picks a category from `home-category-filter`, and `home-secondary-filter` rebuilds its label + options. (4) **Debounced server-side search**: any change to query/city/category/secondary/geo triggers a 300ms debounced hit to `/search/providers` with the assembled params, replacing the previous client-side filter loop. `home-search-clear`, `home-city-clear`, `home-filters-clear` reset the relevant state. Empty-state text now uses `anyFilterActive` flag ("No providers match your filters. Try clearing some.") and doesn't flash while `searching=true`. (5) **Test coverage**: iter44 pytest 18/19 pass (the 19th is a docstring numbering nit — code correct), iter40+41+42+43 regression 42/42 green, frontend E2E via testing_agent_v3_fork verified all 9 UI acceptance criteria (search input, category switching, city typeahead, combined filter surfacing, empty-state phrasing).

- **Iteration 46: HOSPITAL WORKFLOW DISCOVERABILITY (Feb 2026)** — Assistant→doctors mapping + doctor-wise availability were already wired up in iter39/iter45 but hidden behind icon-only affordances. User called this out; two rounds of bug_testing_agent verification later, verdict = fixed (100/100 backend, 100/100 frontend). (1) **User model bug fix**: `assigned_staff_ids: List[str] = []` was missing on the Pydantic `User` — so /providers/me/assistants stripped the field on serialization. Added. (2) **Assistant assign button**: `ProviderAssistants.jsx` now renders a labelled "Assign" pill on every row (was icon-only, hidden when staff=0). Each row also prints `Mapped to N / 3 doctors/services` with a rose "tap Assign to map" hint when N=0. Modal `assign-count` counter blocks the 4th checkbox. Backend PUT still enforces max-3 (400 error). (3) **Availability page callout**: `ProviderAvailability.jsx` now fetches `/providers/me/profile` (not the 404-returning `/providers/me`) and, when `provider_type=='hospital'`, renders a dashed callout at the top explaining that this page holds only the hospital's default schedule + two buttons — "Per-doctor schedules" → `/provider/staff` and "Bulk schedule grid" → `/provider/schedule-grid`. (4) **Backend enrichment**: `GET /providers/me/profile` now joins `db.categories` to attach `category_name` and `category` on the response so `isAutomobileProvider` gating still works everywhere it depends on that field. `ProviderQueue.jsx` and `ProviderServices.jsx` were switched to the same endpoint. (5) **HospitalStaff hero + empty states**: iter45's static hero banner strengthened; empty state for both Doctors and Other Services is now a big dashed clickable CTA that opens the add-modal directly. Onboarding form shows a "Next step: add your doctors & services" callout for Hospital selection (basic-services checklist hidden when hospital picked).

- **Iteration 48: HOSPITAL PROFILE, QUEUE CLARITY, PHONE VISIBILITY, RESEND OTP, WALK-IN STAFF (Feb 2026)** — 5 improvements shipped. (1) **Hospital public detail** (`/provider/:id`): backend `GET /providers/{id}` now returns `staff` (sub-doctors + sub-services) and `has_availability` counts per-staff rules — fixes the "Currently unavailable" false-negative + "No services listed yet" complaint. Frontend `ProviderDetail.jsx` renders "Our Doctors" + "Other Services" clickable cards linking to `/book/:id?staff=<id>`, shows "Call {phone}" pill (`provider-call-btn`) when `contact_phone` is set, hides the empty services block for hospitals, and the sticky CTA reads "Book a slot • pick a doctor / service". (2) **Queue clarity**: `QueueRow.jsx` now shows a `queue-staff-<bookingId>` badge with "Doctor / Service <name>" so the assistant always knows who a token is booked with. (3) **60-sec Resend OTP timer**: `Login.jsx` gained `login-resend-countdown` + `login-resend-otp-btn` — after tapping Continue, the user sees "Resend OTP in Ns" that ticks down every second, then a clickable "Resend OTP" that restarts the 60s cooldown. (4) **Phone visibility**: `HospitalStaff` + `HospitalStaffUpsert` gained `phone` field (round-tripped through `add_my_staff` / `update_my_staff`); provider add-doctor / add-service modal has a `staff-phone` input and the customer sees "📞 <phone>" as a `tel:` link on each sub-doctor / sub-service card. `ProviderOnboarding` gained `onboarding-contact-phone` for the hospital-wide public number. (5) **Hospital walk-in staff picker**: `WalkinCreate.staff_id` + `add_walkin` resolve `staff_name` / `staff_kind` and use an independent per-staff token counter. Receptionist walk-in modal now shows a required "Doctor / Service *" dropdown (`rec-walkin-staff`) for hospitals. (6) **Test coverage**: `/app/backend/tests/test_iter48_hospital_detail_and_walkin_staff.py` (4/4 pass) covers response shape, phone round-trip, walk-in per-staff counter, and 400 on invalid staff_id.

- **Iteration 50: FOCUSED-STAFF FILTER ON RECEPTIONIST DESK (Feb 2026)** — User reported: "Doctor tile click opens modal but main list still mixes all doctors' patients." Fix: added `focusedStaff` state on `ReceptionistDashboard.jsx`. Clicking any doctor/service tile now (a) opens the drill-in modal AND (b) filters the main queue list to ONLY that staff's patients (`b.staff_id === focusedStaff.staff_id`). A prominent focus banner `focused-staff-banner` reads "Focused · Showing only Dr. X's patients" with a `focused-staff-clear` "Show all patients" button. While focused: "Call next" advances only that doctor's queue via `/assistant/queue/next?staff_id=…` and the top-level "Walk-in" button pre-scopes to that staff. The `queue-empty` message + reorder header + SortableContext all use the filtered `visibleQueue`. Verified end-to-end via screenshots (2 seeded walk-ins for 2 different doctors — filter correctly showed only 1 of them; clearing brought both back). All 12 regression pytest cases still pass.

- **Iteration 51: FIX — `/assistant/queue/next` was excluding walk-ins (Feb 2026)** — User reported: "Dr. Rahul Sharma has visible patients in the focused list but Call next says 'Queue is empty for this doctor / service.'" Root cause: `/assistant/queue/next` had `is_walkin: False` hard-coded in its "next active token" lookup, so walk-ins tagged with that staff_id were ignored even though the multi-queue tile counted them. Fix: removed the filter — walk-ins tagged to a doctor are a legitimate part of that doctor's live queue and now advance correctly. Regression test at `/app/backend/tests/test_iter51_assistant_next_includes_walkins.py` (2/2 pass) exercises: 2 walk-ins → first Call next completes walk-in A, second completes walk-in B, third genuinely returns `ok:False`. Also asserts unassigned-staff still returns 403. All 14 iter47/48/49/51 pytest cases green.

- **Iteration 52: EXPLICIT SEARCH BUTTON ON CUSTOMER HOME (Feb 2026)** — User reported: filters visible but "search sahi se kaam nahi kar raha". Fix: replaced the auto-firing 300ms debounced useEffect with an explicit user-triggered `runSearch()` on `Home.jsx`. Now filter chips (category / secondary / city / near-me / name query) stage locally and only actually hit `/search/providers` when the user (a) clicks the new orange `home-search-btn` (Search) next to the search bar, (b) presses Enter in the search input, (c) picks Nearby (auto-fires after GPS resolves) or (d) taps Clear all (returns to unfiltered `/providers`). Search bar + button are wrapped in a `<form>` so Enter naturally submits. Verified via screenshots: staging Healthcare filter did NOT auto-refresh the list; clicking Search did; typing "Sharma" + Enter correctly narrowed to Sharma Clinic + apollo hospital test.

- **Iteration 53–54: MOBILE-APP SCHEMA ALIGNMENT + PROVIDER_TYPE VALUE MIGRATION (Feb 2026)** — User confirmed the SlotNow Web backend must share the exact same MongoDB schema as the mobile app (`Book Preview 11`) so both apps hit the shared `book-preview-11` database without transformation. Aligned schema:

- **Iteration 55: PIN LENGTH + PIN LOGIN FLOW + BOOKING TIME (Feb 2026)** — Post cross-app DB share, user reported 3 issues seen on production (`slotnow.co.in` reading Book Preview 11's DB):
  1. **PIN length 6 → 4 digits (frontend enforcement)**: `Login.jsx` PIN input `slice(0, 4)` + submit disabled unless `pin.length === 4`; set-pin regex `/^\d{4}$/`. `Profile.jsx` change-PIN modal + `savePin` matched. All PIN placeholders and i18n labels (`enter_pin` EN + HI) updated from "4-6 digit PIN" → "4-digit PIN".
  2. **PIN login fails after set-pin (phone format mismatch)**: Root cause — web frontend was sending `phone = toIndianE164(phone)` (12-digit `91XXXXXXXXXX`) but the mobile app stores users with **bare 10-digit** phone per its spec. Mobile backend's phone lookup didn't try the 12-digit variant, so PIN login queries missed the mobile-created row and returned "Invalid phone or PIN". Fix: new helper `toBareIndianPhone()` in `/app/frontend/src/lib/phone.js` and `AuthContext.jsx` now uses it for `sendOtp`, `verifyOtp`, `pinLogin` — cross-app login now finds the same user record.
  3. **Booking time display wrong**: `formatTime(hhmm)` was splitting a string on `:`. When Book Preview 11 stored the booking time as a full ISO timestamp (e.g. `2026-08-15T09:30:00Z`) instead of the bare `HH:MM` web expects, the split produced `NaN:30`. Fix: `formatTime` now detects ISO/timestamp inputs (`T` present or length > 5), parses them via `new Date()` and reads `getHours()` / `getMinutes()` — displays in the user's local timezone.
  Verified via preview screenshots (PIN input correctly caps at 4 digits and label reads "4-digit PIN"). Backend regression tests still pass (backend .env kept on local Mongo for preview isolation). Ready for production redeploy.

  1. **Collection rename**: `hospital_staff` → `staff` (43 references in `server.py` refactored; frontend `ProviderAnalytics.jsx` response key `hospital_staff` → `staff`).
  2. **Field rename**: `hospital_id` → `provider_id` on staff docs. All API filters + insert paths updated. Public `/providers/{id}/staff` endpoint renamed internally (`public_hospital_staff` → `public_provider_staff`), URL preserved.
  3. **Model class rename**: `HospitalStaff` → `Staff`, `HospitalStaffUpsert` → `StaffUpsert`. Added optional `designation: Optional[str] = ""` field to match mobile spec.
  4. **provider_type value migration**: `doctor_clinic` → `clinic`, `diagnostic_center` → `service` (hospital unchanged). Backend `/reference/healthcare` returns mobile-parity keys. Frontend `ProviderOnboarding.jsx` updated to check `"clinic"` and `"service"` instead of legacy values. **Migration**: idempotent script at `/app/backend/scripts/migrate_iter54_provider_type.py` + backend startup hook auto-runs both iter53 (`staff.hospital_id` → `provider_id`) and iter54 (`providers.provider_type` renames) on every boot — safe to re-run, no-op when clean.
  5. **PIN parity**: web already used `pin_hash` + `has_pin` + `bcrypt.gensalt(rounds=12)` — verified byte-for-byte identical to mobile spec. `GET /admin/users` and `/admin/referrals` were leaking `pin_hash` in raw docs → added `{"pin_hash": 0}` projections. Cross-hash test proves a mobile-generated hash verifies via web's `verify_pin()`.
  6. **Local data migration**: 7 legacy `hospital_staff` docs auto-migrated into `staff` on preview DB via one-time script. Production DB will migrate on first boot after redeploy.
  7. **Test coverage**: `/app/backend/tests/test_iter53_schema_alignment_mobile.py` (8/8 pass) + `test_iter54_provider_type_alignment.py` (3/3 pass) locking down all cross-app invariants. Total regression: **25/25 pytest cases green** (iter47 + 48 + 49 + 51 + 53 + 54).





- **Iteration 49: RECEPTIONIST DESK OVERHAUL — DATE NAVIGATION + HOSPITAL TOTALS + PER-STAFF DRILL-IN (Feb 2026)** — 3 assistant-desk workflow improvements shipped. (1) **Date navigation strip**: `ReceptionistDashboard.jsx` renders a 7-day strip (`receptionist-date-strip`) with cells for −3, −2, −1, TODAY, +1, +2, +3 days (`date-cell-<iso>`). Selected date drives the `/queue/today?date=`, `/assistant/queue/multi?date=` and `/queue/walkin?date=` calls so the assistant can review past days or pre-book future walk-ins. Live-polling stays enabled only on today's date. Header pill (`receptionist-selected-date`) shows "TODAY" or the picked ISO date. (2) **Hospital total badge**: multi-queue backend now returns `hospital_total` + `hospital_active` + `hospital_completed`; the multi-queue strip has a green `hospital-total-badge` pill (e.g. "Total today: 12 · 4 active") right next to the section heading. (3) **Per-doctor drill-in modal** (`StaffQueueModal.jsx`): every doctor/service tile (`aq-tile-{id}`) is now a button that opens a full-screen modal with stats (Now serving / In queue / Completed), a `staff-queue-call-next` button, a `staff-queue-add-walkin` button that opens the shared walk-in modal pre-scoped to that staff, and the full patient list split into "In queue" / "Completed". New endpoint `GET /assistant/staff/{staff_id}/queue?date=` returns the enriched booking list scoped to a single staff + date. `/assistant/queue/multi` no longer filters out walk-ins so staff tiles now count walk-ins tagged with that staff_id. (4) **Test coverage**: `/app/backend/tests/test_iter49_receptionist_desk.py` — 4/4 pytest pass (hospital totals, per-staff drill-in with cross-staff leak check, walk-in `?date=` param, per-staff date scoping). Regression: iter47 + iter48 still green.


- **Iteration 47: ASSISTANT LOGIN BUG FIX + ASSISTANT PHOTO UPLOAD (Feb 2026)** — User reported P0 blocker: Service Assistant login threw "Unauthorized. Ask your service provider to add you as an assistant." despite the provider having added them. Root cause: `create_assistant` stored the raw phone string sent by the frontend (10-digit OR 12-digit) and did exact-string matching during upsert — same person could exist as two rows and login-lookup landed on the row without `linked_provider_id`. (1) **Backend fix**: `create_assistant` now normalises every incoming phone to `91XXXXXXXXXX` via `normalize_indian_phone()` before storing AND checks for existing receptionist rows across all three variants (raw / 12-digit / 10-digit) so upserts always hit the same row. `verify_otp` phone lookup now also spans all three variants (matches `pin_login` semantics). (2) **Assistant photo upload**: `User` + `CreateAssistantRequest` gained optional `photo` (data URL). `ProviderAssistants.jsx` add-form has a new `assistant-photo-input` file picker (compressed to 512px via `compressImageToDataURL`) with live preview + remove. Each list row shows the photo instead of an initial-avatar and gets a small camera-icon overlay (`assistant-photo-edit-{id}`) for one-tap photo update via the same upsert endpoint. `ReceptionistDashboard.jsx` hero card now shows `user.photo` (with `receptionist-self-photo`) + `receptionist-self-name` + designation so patients / other staff can visually identify the assistant at the counter. (3) **Test coverage**: `/app/backend/tests/test_iter47_assistant_login_and_photo.py` (4/4 pass) covers phone normalization variants, 10-digit + 12-digit login parity, upsert-not-duplicate on format change, and orphan receptionist still-rejected. `bug_testing_agent` verdict = fixed (100% backend, 100% frontend).











- **Iteration 35: PROVIDER TYPES + SEARCH + CITY SEO + VERIFIED BADGE (Feb 2026)** — big feature batch. (1) **ProviderProfile schema extended**: `provider_type` ("hospital"/"doctor_clinic"/"diagnostic_center"), `specialization` (Neurologist etc.), `service_tags` (["X-ray","MRI",…]). Backend `GET /api/reference/healthcare` returns 3 provider types + 16 specializations + 12 diagnostic services for frontend dropdowns. (2) **Backend search endpoint** `GET /api/search/providers?q=&city=&specialization=&service=&lat=&lng=&max_km=` — combines text/city/specialization/service filters and, when lat+lng given, applies Haversine distance filter (default 25km) + sorts by distance ascending (adds `distance_km` to each result). (3) **Public `/city/:cityName` SEO page** — backend `GET /api/city/:slug` groups all approved providers in a city by category (ItemList schema). Frontend `CityPublicPage` renders "Services in Mumbai" hero + category groups + provider cards. SEO: dynamic title = "Mumbai — Doctors, Salons, Garages & more on SlotNow", canonical, JSON-LD Place schema. Backend sitemap now includes `/city/:slug` for each city that has approved providers. (4) **`/search` customer search page** — city dropdown (aggregated from providers), specialist doctor dropdown, service dropdown, keyword input, geolocation "Find nearby" button (uses `navigator.geolocation`). Results sorted by rating (or distance when nearby active). Filters persist in URL params for shareability. Added Search link to landing header and "Find near me" CTA to hero. (5) **Provider onboarding** now shows healthcare-specific fields when category = Healthcare: `provider_type` selector, `specialization` dropdown (only for doctor_clinic), `service_tags` multi-select chips (for hospital + diagnostic_center). (6) **Verified booking badge**: reviews now display "Verified booking" green tick since every review has a `booking_id` linked to a completed booking (schema guarantee). ShieldCheck icon + tooltip. Verified via screenshots — city page shows 3 groups + 3 providers; search page filters work with 7 providers; specialization/service dropdowns populated from reference API.


## Open Backend Recommendations (for backend team)
- **[NEW – would let us remove a workaround] Weekday convention on `Provider.availability`**: the backend stores/reads weekday using Python's `datetime.weekday()` convention (0=Mon..6=Sun). The web frontend uses JS `getDay()` (0=Sun..6=Sat), so we now bridge with `jsToPyWeekday()`/`pyToJsWeekday()` in ProviderAvailability.jsx. If the backend is ever migrated to a shared 0=Sun..6=Sat convention (matching mobile clients / most calendar APIs), both helpers can be deleted together.
- **[NEW – would let us remove a workaround] `/queue/today` provider serialization**: currently returns `provider.category` as a display STRING (e.g. "Automobile") instead of `provider.category_id`. The web frontend now string-matches "automobile" to decide whether to show the vehicle walk-in branch, which is fragile against localization. Please add `category_id` to the response.
- **[NEW – would let us remove a workaround] `GET /api/queue/my-position`**: (a) add an explicit `position` integer (count of active tokens strictly ahead of the caller), (b) honor the `provider_id` and `date` query params — currently the endpoint returns the caller's next active booking globally, so a customer viewing a *non-first* active booking gets misleading data. Frontend workaround (iteration 14): derive `position = qp.wait` and only trust the response when `qp.booking.id === viewed booking id`.
- **[NEW – would let us remove a workaround] `Provider.location_link`**: add a dedicated string column so we can stop piggy-backing the Google Maps link inside `address`. Frontend workaround (iteration 14): `packAddress(text, url) → "<text>\n\n📍 <url>"`, `unpackAddress()` on read.
- **[Security] Migrate auth token storage to httpOnly cookies.** The web frontend currently stores JWT in `localStorage` because the deployed backend returns `{ token }` in JSON. localStorage is XSS-exposed. Backend must (a) issue the JWT via `Set-Cookie: HttpOnly; Secure; SameSite=Lax` on `/api/auth/verify-otp` and `/api/auth/pin-login`, (b) accept the cookie on subsequent requests, (c) add a `/api/auth/logout` that clears the cookie, and (d) enable CORS `credentials: true`.
- Add a dedicated `PUT /api/admin/providers/{id}/suspend` (+ `/unsuspend`) endpoint. Currently Suspend reuses `/reject`, which is semantically fragile.
- Implement `GET /api/referrals/mine` returning `{ count: N }` (or add `total_refs` to `/users/me`) so the Profile progress bar shows live counts. Currently 404s; UI handles gracefully.
- Consider a real image upload endpoint (multipart) or return a signed URL. Current approach stores base64 in Mongo which may bloat documents; fine for MVP but a proper media service would be cleaner.
- Consider explicit `approved: false` on freshly-onboarded providers so they enter the admin approval funnel.
- Seed a receptionist account linked to an Automobile provider so the vehicle-fields walk-in branch can be validated live (currently the sole receptionist is linked to a Healthcare provider, so the auto branch remains code + route-intercept verified only).

## Backlog (P2/P3)
- Real WebSocket-based live queue.
- Emergent-managed Google Auth.
- Provider avatar upload (currently business image only).
- Referral rewards ledger (backend needs schema + rules).

## Test credentials
- Customer: `9999999999` / OTP `123456` / PIN `1234`
- Provider (onboarded): `8888800884` / OTP `123456`
- Provider (QuickWheels, Automobile, approved): `8888800002` / OTP `123456`
- Service Assistant: `6666600011` / OTP `123456`
- Admin: `7777700002` / OTP `123456`
- Fresh provider (parked on onboarding): `8782913356` / OTP `123456`
- Pending providers for bulk-approve demo: `91111001`, `91111002`, `91111003`
