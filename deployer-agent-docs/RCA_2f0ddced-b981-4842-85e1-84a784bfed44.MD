# RCA — "pro-booking-21 stuck on old build" / empty Provider Type dropdown

## TL;DR (the ownership question, answered)
`pro-booking-21.emergent.host` is **NOT** this project. This agent is scoped to a **different** deployment:

- **This project** = `slotnow-web`
  - deployment_id: `8329e665-97b3-4436-be4a-7dca974059be`
  - live URL: `https://slotnow-web.emergent.host`
  - custom domains: `slotnow.co.in`, `www.slotnow.co.in` (both verified)
  - template: `fastapi_react_mongo_shadcn_base_image` · frontend: cloudflare · cluster: `target-2`
  - latest run: `2f0ddced-b981-4842-85e1-84a784bfed44` (redeploy, success, 2026-08-26 15:48Z)

- **`pro-booking-21.emergent.host`** = a **separate/older Emergent project (deploy target #B)**. It is not attached to this deployment (not its `deployed_url`, not in its domains list) and is **not reachable from any tool in this pod.** I cannot inspect it, deploy to it, or change its domains.

## The model is inverted — this is the key correction
The plan assumed `slotnow.co.in` is the "stale duplicate." It is the opposite:

- Verified live on THIS project: `GET https://slotnow-web.emergent.host/api/reference/healthcare` → **HTTP 200** with the correct payload:
  `provider_types: [hospital, clinic, service]`, full `specializations` + `services` lists.
- That is exactly the data that populates the "Become a Provider → Provider Type" dropdown.
- `slotnow.co.in` is served by THIS project (it's a custom domain here), so **`slotnow.co.in` already has the iteration 35–55 code.**
- `pro-booking-21.emergent.host` is the one **missing** the endpoints (404s) → it is the **older/stale** project, on its **own separate MongoDB cluster** (explains the different category IDs / fact #8).

So: the up-to-date code lives HERE (slotnow-web / slotnow.co.in). The stale backend is pro-booking-21 — which is also where all the real Users/Providers/Staff/Bookings + the live mobile writes are.

## Why the dropdown is empty on production
The mobile app + the frontend the user is testing point at `pro-booking-21.emergent.host`, whose backend image predates iterations 35–55 and has no `/api/reference/healthcare` (404). Frontend renders an empty dropdown because the reference call 404s. No frontend rebuild is needed — only the pro-booking-21 backend needs the new `server.py`.

## What I can and cannot do from here
- CANNOT deploy the new `server.py` to `pro-booking-21` — it's a different project, out of my scope.
- CANNOT move/retire the `slotnow.co.in` custom domain, and CANNOT point it at pro-booking-21 — no domain link/unlink tool exists in this surface (that's a UI action), and it is a cross-project move.
- Deploying THIS project (`slotnow-web`) would NOT help — it publishes to `slotnow-web.emergent.host` / `slotnow.co.in`, not to `pro-booking-21`, and it is on a different DB cluster (`target-2`). So I have deliberately run **no deploy**.

## Recommended plan (data-safe, matches the user's real intent)
The goal ("one shared backend at pro-booking-21, keep its DB, mobile keeps writing") is achieved by **updating pro-booking-21's backend code in place from ITS OWN project session** — NOT by moving domains and NOT from here:

1. Open the Emergent project that OWNS `pro-booking-21.emergent.host` (its own build/preview session). Identify it by its deployment panel URL / slug — it is a distinct project from `slotnow-web` (`8329e665-…`). This pod cannot enumerate other projects, so the user/E1 must select it.
2. In that project, deploy the latest `server.py` (~3684 lines) so `/reference/healthcare`, `/reference/cities`, `/city/{slug}`, `/search/providers`, `/healthz`, `/public/waiting/{provider_id}`, and the aligned `staff` endpoints (POST/PATCH `/providers/me/staff` with `cabin`) go live there. A **redeploy preserves that project's existing MongoDB** — no migration, no drop, mobile writes keep flowing. This satisfies "DO NOT migrate/lose data."
3. Do NOT retire `slotnow.co.in` by moving it to pro-booking-21. `slotnow.co.in` is served by THIS (current-code) project; retiring/moving it would (a) break the good deployment and (b) point users at the still-stale pro-booking-21 until step 2 is done. If the user genuinely wants a single hostname, do the domain change from the UI only AFTER pro-booking-21 has the new code — and it's a Deployment-panel domain operation, not something this agent performs.
4. The two DBs are on separate clusters. If the user later wants slotnow-web's data folded into pro-booking-21, that's a deliberate data operation — out of scope here and explicitly not to be auto-run given the "do not migrate/lose" constraint.

## Bottom line
- Right code is on `slotnow-web` (`slotnow.co.in`). Stale backend is `pro-booking-21` (separate project + separate DB).
- Fix = redeploy latest `server.py` inside the pro-booking-21 project's own session (preserves its DB). Cannot be done from this project's agent.
- No deploy and no domain/DB change was performed by me.
